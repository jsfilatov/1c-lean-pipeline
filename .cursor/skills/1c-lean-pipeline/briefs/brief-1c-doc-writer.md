## Документация (пользователь / админ)

**Вызов только после gate `g_doc`:** оркестратор формирует `{{HANDOFF_JSON}}` и вызывает этот субагент **только после** того, как пользователь в чате **явно закрыл** `g_doc` согласием на фазу 9 (см. `SKILL.md` — § «Фаза 9 и gate `g_doc`»). До ответа пользователя handoff фазы 9 **не создавать**.

## Машинный handoff (JSON)

- **Запрос:** `{{HANDOFF_JSON}}` — JSON, соответствующий схеме `.cursor/skills/1c-lean-pipeline/schemas/handoff-request.schema.json`. Сначала используй `slices`, `anchors`, `read_paths`; полные gate-файлы читай по `gate_documents.*.path`, если среза недостаточно или есть риск устаревания после правок пользователя на gate (ориентир — `source_revision` в JSON и фактическое содержимое файла).
- **Ответ (опционально):** если оркестратор подставил непустой путь `{{HANDOFF_RESPONSE_JSON}}`, запиши туда JSON по схеме `.cursor/skills/1c-lean-pipeline/schemas/handoff-response.schema.json`; иначе достаточно раздела «Ответ в чат» ниже.

- **Каталог задачи:** {{TASK_DIR}}
- **Фокус:** {{FOCUS}}

## Обязательно прочитай

1. `{{HANDOFF_JSON}}` — срезы по продукту и таскам, `read_paths`.
2. `gate_documents.proposal.path`, `gate_documents.design.path`, `gate_documents.tasks.path`, `spec_deltas[]` — при необходимости полного текста.
3. Согласованные с пользователем пути для документации (оркестратор укажет в дополнении к брифу)

## Результат

Обнови или создай документы **вне прикладного кода** или в заранее согласованных каталогах. Inline-комментарии в BSL — зона разработчика, не дублируй их как «документацию продукта».

## Ответ в чат

Список файлов документации + 3–7 строк.
